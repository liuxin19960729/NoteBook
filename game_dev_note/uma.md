```js


// 远程配置插件安装
lt.use(At);


lt.use(Vt);


uma.ini(options)

init(){
    if(useOptionid){ 
      // 提示 手动设置openid 日子 
    }

    lt.init(options);
}


lt.init(options){
    // 判断是否已经初始化
    if(this._inited) {
      
    }else if(options){
        if(options.appKey){
           
           
           //判断是否设置 useOpenid 若果value 不是 boolen 类型 默认设置为 true
            


            // 缓存  options 
            

            // debug 值


            this._inited=true



            // 发送事件
            emit(r.messageType.CONFIG_LOADED,options)


            load(function(){

            });


        }
    }
}

function load(func){

            if (i) {
                    a.removeStorage(e);
                    t()
                } else {
                    // storage Key
                    e = "um_cache_" + n().appKey();
                    a.getStorage(e, (function (n) {
                        i = y(n) || {};
                        r = !0;
                        a.removeStorage(e);
                        t()
                    }))
                }
}



```